
services:
  # Database Service (MySQL 8.0)
  db:
    image: mysql:8.0
    container_name: pms-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_NAME:-pms_database}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rootpassword}
      MYSQL_USER: ${DB_USER:-pms_user}
      MYSQL_PASSWORD: ${DB_PASSWORD:-password}
    command: >
      --innodb-buffer-pool-size=256M
      --max-connections=200
      --innodb-log-file-size=64M
      --log-bin-trust-function-creators=1
    ports:
      - "3307:3306"
    volumes:
      - pms_db_data:/var/lib/mysql
    networks:
      - pms-network
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "sh", "-c", "mysqladmin ping -h localhost -u root -p$$MYSQL_ROOT_PASSWORD"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # Cache & Queue Service (Redis)
  redis:
    image: redis:7-alpine
    container_name: pms-redis
    restart: unless-stopped
    # BullMQ stores queued/repeatable jobs as Redis keys with no TTL, so any
    # eviction policy can silently drop pending background work. Everything else
    # on this instance (cache, rate limits, locks) sets its own TTL, so nothing
    # here depends on eviction to reclaim memory.
    command: >
      redis-server
      --maxmemory 128mb
      --maxmemory-policy noeviction
      --activedefrag yes
    ports:
      - "6379:6379"
    volumes:
      - pms_redis_data:/data
    networks:
      - pms-network
    deploy:
      resources:
        limits:
          # Kept at 2x --maxmemory: RDB snapshots fork the process, and
          # copy-on-write can push RSS well above the dataset size. At the
          # previous 160M a full 128mb dataset risked a Docker OOM-kill,
          # which drops queued jobs — the very thing noeviction prevents.
          memory: 256M
        reservations:
          memory: 64M
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "2"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # Backend Service (Node.js Express)
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: pms-backend
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      - DB_HOST=db
      - DB_USER=${DB_USER:-pms_user}
      - DB_PASSWORD=${DB_PASSWORD:-password}
      - DB_NAME=${DB_NAME:-pms_database}
      - JWT_SECRET=${JWT_SECRET}
      - CLOUDINARY_CLOUD_NAME=${CLOUDINARY_CLOUD_NAME}
      - CLOUDINARY_API_KEY=${CLOUDINARY_API_KEY}
      - CLOUDINARY_API_SECRET=${CLOUDINARY_API_SECRET}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - STRIPE_PUBLISHABLE_KEY=${STRIPE_PUBLISHABLE_KEY}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - REDIS_HOST=redis
      - PORT=3000
      - FRONTEND_URL=${FRONTEND_URL:-http://localhost:5173}
      - VITE_ENABLE_PAYMENT_SIMULATION=${VITE_ENABLE_PAYMENT_SIMULATION:-false}
      - ENABLE_PAYMENT_SIMULATION=${ENABLE_PAYMENT_SIMULATION:-false}
      - UV_THREADPOOL_SIZE=8
    ports:
      - "3000:3000"
    volumes:
      - pms_uploads_data:/app/uploads
      - pms_logs_data:/app/logs
    networks:
      - pms-network
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 128M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: pms-worker
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      - DB_HOST=db
      - DB_USER=${DB_USER:-pms_user}
      - DB_PASSWORD=${DB_PASSWORD:-password}
      - DB_NAME=${DB_NAME:-pms_database}
      - JWT_SECRET=${JWT_SECRET}
      - CLOUDINARY_CLOUD_NAME=${CLOUDINARY_CLOUD_NAME}
      - CLOUDINARY_API_KEY=${CLOUDINARY_API_KEY}
      - CLOUDINARY_API_SECRET=${CLOUDINARY_API_SECRET}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - STRIPE_PUBLISHABLE_KEY=${STRIPE_PUBLISHABLE_KEY}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - REDIS_HOST=redis
      - PORT=3000
      - UV_THREADPOOL_SIZE=8
    command: node worker.js
    volumes:
      - pms_uploads_data:/app/uploads
      - pms_logs_data:/app/logs
    networks:
      - pms-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 64M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  # Frontend Service (Vite + Nginx)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: pms-frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "5173:8080"
    networks:
      - pms-network
    deploy:
      resources:
        limits:
          memory: 64M
        reservations:
          memory: 16M
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "2"
  # Data Warehouse Database (PostgreSQL)
  dwh-db:
    image: postgres:15-alpine
    container_name: pms-dwh-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DWH_DB_NAME:-pms_dwh}
      POSTGRES_USER: ${DWH_DB_USER:-dwh_user}
      POSTGRES_PASSWORD: ${DWH_DB_PASSWORD:-dwh_password}
    ports:
      - "5432:5432"
    volumes:
      - dwh_db_data:/var/lib/postgresql/data
    networks:
      - pms-network
    deploy:
      resources:
        limits:
          memory: 512M
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DWH_DB_USER:-dwh_user} -d ${DWH_DB_NAME:-pms_dwh}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # BI Tool (Metabase)
  metabase:
    image: metabase/metabase:latest
    container_name: pms-metabase
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      MB_DB_TYPE: postgres
      MB_DB_DBNAME: ${DWH_DB_NAME:-pms_dwh}
      MB_DB_PORT: 5432
      MB_DB_USER: ${DWH_DB_USER:-dwh_user}
      MB_DB_PASS: ${DWH_DB_PASSWORD:-dwh_password}
      MB_DB_HOST: dwh-db
    networks:
      - pms-network
    depends_on:
      dwh-db:
        condition: service_healthy

  # ETL Worker (Node.js)
  etl-worker:
    build:
      context: ./etl
      dockerfile: Dockerfile
    container_name: pms-etl-worker
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      dwh-db:
        condition: service_healthy
    environment:
      # Source DB
      - SRC_DB_HOST=db
      - SRC_DB_USER=${DB_USER:-pms_user}
      - SRC_DB_PASSWORD=${DB_PASSWORD:-password}
      - SRC_DB_NAME=${DB_NAME:-pms_database}
      # Dest DB
      - DWH_DB_HOST=dwh-db
      - DWH_DB_USER=${DWH_DB_USER:-dwh_user}
      - DWH_DB_PASSWORD=${DWH_DB_PASSWORD:-dwh_password}
      - DWH_DB_NAME=${DWH_DB_NAME:-pms_dwh}
      # CRON
      - ETL_CRON_SCHEDULE=0 1 * * *
    networks:
      - pms-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  pms-network:
    driver: bridge

volumes:
  pms_db_data:
  pms_uploads_data:
  pms_logs_data:
  pms_redis_data:
  dwh_db_data:
